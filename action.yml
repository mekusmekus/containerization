name: "my first container action"
description: "this is my practise"
author: "Tom"

inputs:
  myInput:
    description: "greeting to use"
    required: true
    default: "Awesome guru"
outputs:
  myoutput:
    description: "total memory"
    
runs:
  using: "docker"
  image: "Dockerfile
